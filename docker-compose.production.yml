
version: '3.4'

services:

  django:
    build: .
    # Loading the app is defined here to allow for
    # autoreload on changes it is mounted on top of the
    # old copy that docker added when creating the image
    volumes:
      - '.:/usr/src/app'
    environment:
      - DEBUG=False
      - GEONODE_LB_HOST_IP=geoportal.watertech.vn
      - GEONODE_LB_PORT=80
      - SITEURL=https://geoportal.watertech.vn/
      - ALLOWED_HOSTS=['*', ]
      - GEOSERVER_PUBLIC_LOCATION=https://geoportal.watertech.vn/geoserver/
      - GEOSERVER_WEB_UI_LOCATION=https://geoportal.watertech.vn/geoserver/

  celery:
    build: .
    volumes:
      - '.:/usr/src/app'
    environment:
      - DEBUG=False
      - GEONODE_LB_HOST_IP=geoportal.watertech.vn
      - GEONODE_LB_PORT=80
      - SITEURL=https://geoportal.watertech.vn/
      - ALLOWED_HOSTS=['*', ]
      - GEOSERVER_PUBLIC_LOCATION=https://geoportal.watertech.vn/geoserver/
      - GEOSERVER_WEB_UI_LOCATION=https://geoportal.watertech.vn/geoserver/


  geonode:
    environment:
      - HTTP_HOST=geoportal.watertech.vn

  geoserver:
    environment:
      - GEONODE_LB_HOST_IP=geoportal.watertech.vn
      - GEONODE_LB_PORT=80
  #    - NGINX_BASE_URL=